<?xml version="1.0" encoding="utf-8"?>
<!--
  Copy for the Chain settings page.

  Its own file on purpose: strings.xml is already large, and the chain feature's
  wording is the part most likely to be revised as the cores change. One file per
  language keeps a translation diff readable.
-->
<resources>

    <!-- Named once at the top of the page, so a build's contents are not a
         connect-time surprise. %1$s is a comma-separated list of core names. -->
    <string name="chain_build_missing">This build does not include: %1$s. Modes that need them are disabled.</string>

    <string name="chain_group_single">One core</string>
    <string name="chain_group_stacked">Stacked cores</string>
    <string name="chain_group_stacked_note">Each extra hop adds latency. Reach for these when the simple path is the thing being blocked, not before.</string>

    <!-- %1$s is a comma-separated list of core names. -->
    <string name="chain_mode_unavailable">Not available: this build has no %1$s core.</string>

    <string name="chain_tier_fast">Fast</string>
    <string name="chain_tier_moderate">Slower</string>
    <string name="chain_tier_slow">Slowest</string>

    <string name="chain_psiphon_options">Psiphon</string>
    <string name="chain_tor_options">Tor</string>

    <string name="psiphon_config_pasted">Using the config you pasted below.</string>
    <string name="psiphon_config_bundled">A Psiphon config is bundled with this build. Paste one below only to override it.</string>
    <string name="psiphon_config_absent">This build has no Psiphon config. Paste one below, or the Psiphon modes cannot start.</string>
    <string name="psiphon_config_invalid">That config is not valid JSON, so it will be ignored.</string>
    <string name="psiphon_config_invalid_help">Paste the whole config, from the opening brace to the closing one.</string>
    <string name="psiphon_config_clear">Clear config</string>

    <string name="tor_tradeoffs">Tor is slow: the first connection regularly takes two minutes or more. It also carries no UDP, so video calls will not work through it.</string>
    <string name="tor_exit_no_geoip">This build has no Tor country database, so an exit country cannot be requested.</string>

</resources>
